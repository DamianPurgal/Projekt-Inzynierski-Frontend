import { BlackboardSortPipe } from './blackboard-sort.pipe';

describe('BlackboardSortPipe', () => {
  it('create an instance', () => {
    const pipe = new BlackboardSortPipe();
    expect(pipe).toBeTruthy();
  });
});
