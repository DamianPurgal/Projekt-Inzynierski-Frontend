import { ContributorSortPipe } from './contributor-sort.pipe';

describe('ContributorSortPipe', () => {
  it('create an instance', () => {
    const pipe = new ContributorSortPipe();
    expect(pipe).toBeTruthy();
  });
});
